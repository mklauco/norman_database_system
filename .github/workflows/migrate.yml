name: Migrate Production DB

# Manual-only. Never runs automatically. Trigger from:
#   GitHub → Actions → "Migrate Production DB" → Run workflow
#
# Runs `php artisan migrate --force` against the database in production's
# shared/.env, inside the running nds-app container, against whatever code
# is in `current/` at that moment. Always pull/deploy the new code FIRST,
# then run this workflow second.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type "MIGRATE PRODUCTION" exactly to proceed.'
        required: true
        type: string
      pretend:
        description: 'Run --pretend (show SQL, change nothing).'
        required: true
        type: boolean
        default: false

concurrency:
  group: migrate-production
  cancel-in-progress: false

jobs:
  migrate:
    name: Run migrations on production
    runs-on: ubuntu-latest
    timeout-minutes: 20

    # No `environment:` gate on purpose. It used to point at
    # `production-migrate`, whose required-reviewers rule made every migration
    # wait for a manual approval — a second one, after the Deploy workflow's
    # `production` approval had already reviewed the very same commits. The
    # approval control is also not rendered by the GitHub mobile app, so a
    # dispatched run could sit waiting with no visible way to release it.
    #
    # Neither environment carried any secrets (all four DEPLOY_* secrets are
    # repo-level), so dropping the reference costs nothing. What still guards
    # this workflow:
    #   - the typed "MIGRATE PRODUCTION" confirmation input below
    #   - the check that refuses to migrate unless production is already
    #     serving this exact commit
    #   - workflow_dispatch only: it never fires on its own

    steps:
      - name: Verify confirmation phrase
        run: |
          if [[ "${{ inputs.confirm }}" != "MIGRATE PRODUCTION" ]]; then
            echo "Confirmation phrase did not match. Aborting."
            exit 1
          fi

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          if [ -n "${{ secrets.DEPLOY_KNOWN_HOSTS }}" ]; then
            echo "${{ secrets.DEPLOY_KNOWN_HOSTS }}" > ~/.ssh/known_hosts
          else
            ssh-keyscan -4 -p "${{ secrets.DEPLOY_SSH_PORT || '22' }}" -H "${{ secrets.DEPLOY_SSH_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null
          fi
          chmod 644 ~/.ssh/known_hosts

      - name: Run migrations on production
        env:
          DEPLOY_SSH_HOST: ${{ secrets.DEPLOY_SSH_HOST }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          DEPLOY_SSH_PORT: ${{ secrets.DEPLOY_SSH_PORT || '22' }}
          PRETEND: ${{ inputs.pretend }}
          GIT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          ssh -4 -i ~/.ssh/deploy_key \
              -p "$DEPLOY_SSH_PORT" \
              -o StrictHostKeyChecking=yes \
              -o ConnectTimeout=30 \
              -o ServerAliveInterval=15 \
              -o ServerAliveCountMax=4 \
              "$DEPLOY_SSH_USER@$DEPLOY_SSH_HOST" \
              PRETEND="$PRETEND" GIT_SHA="$GIT_SHA" bash -s <<'REMOTE'
          set -euo pipefail
          cd /opt/projects/norman_database_system

          [[ -L current ]] || { echo "current/ symlink missing — bootstrap not run."; exit 1; }

          ACTIVE="$(readlink current)"
          DEPLOYED_SHA="$(git -C current rev-parse HEAD)"
          echo "Current release: $ACTIVE ($DEPLOYED_SHA)"

          # A half-finished deploy leaves current/ pointing at the previous
          # release. Migrating then reports a green "Nothing to migrate" while
          # the new migrations are silently skipped (2026-09-04). Compare the
          # deployed commit itself, not the release directory name.
          if [[ "$DEPLOYED_SHA" != "$GIT_SHA" ]]; then
            echo "Production is running $DEPLOYED_SHA, not $GIT_SHA."
            echo "Deploy first, then re-run this workflow from the branch you deployed."
            exit 1
          fi

          echo "App version: $(docker exec nds-app php artisan --version)"
          echo ""
          echo "Pending migrations:"
          docker exec nds-app php artisan migrate:status | tail -50
          echo ""

          if [[ "$PRETEND" == "true" ]]; then
            echo "=== PRETEND MODE — showing SQL only, not executing ==="
            docker exec nds-app php artisan migrate --pretend --force
          else
            echo "=== EXECUTING MIGRATIONS ==="
            docker exec nds-app php artisan migrate --force
            echo ""
            echo "Post-migration status:"
            docker exec nds-app php artisan migrate:status | tail -20
          fi
          REMOTE
